{% set menuItems = render_header_menu(3) %}

<ul>
    {% for item in menuItems %}
        <li>{{ item.name }}</li>
        {% if item.children %}
            {% for child in item.children %}
                {{ child.name }}
            {% endfor %}
        {% endif %}
    {% endfor %}
</ul>